# Release tag used to build the GitHub download URLs (app/data.ts).
# Must match the git tag of the databara-app release, including the leading "v".
# Required: if unset, the download URLs will contain "undefined" and break.
NEXT_PUBLIC_VERSION=1.0.4
